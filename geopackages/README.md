# GeoPackages

`teaching_layers.gpkg` is a lightweight SQLite teaching package containing GeoJSON payloads for admin boundaries, roads, and health facilities. The notebooks rely primarily on CSV/GeoJSON for Pyodide compatibility and use this folder to demonstrate data organization.
